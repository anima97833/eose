# 每日三餐美食手账（Meal Diary & Food Album）实施规划

本方案旨在为角色主页右侧新增**美食手账入口图标**（对齐图1可爱的兔兔和果子餐盘），并实现点击后弹出的**每日三餐美食图鉴看板**（深度对齐图2九宫格拱门徽章、星级评价与上方横条详情），支持用户自主上传菜品照片、记录早中晚餐、打分与一句话点评，依托 IndexedDB 实现永久存储。

---

## 1. 深度评估：优点、缺憾与扩展空间

### 优点（Advantages）
1. **治愈烟火气与生活仪式感（Daily Grounding）**：
   - 目前角色主页已有六维、技能、职业、关卡任务，偏向“努力奋斗与成长”。新增【三餐美食手账】将“好好吃饭、热爱生活”具象化，让原本枯燥的日常饮食变成充满收集成就感的 RPG 乐趣。
2. **极致日系和风美学（Badge Album Aesthetics）**：
   - 图2的 3×3 拱门窗格（Arched Windows）、蝴蝶结缎带与星级标注（⭐⭐⭐）极具视觉吸引力，相比普通的列表或打卡日记，能给用户带来极高品质的“收集册”满足感。
3. **与 RPG 养成天然协同（Synergy with HP & Buffs）**：
   - “吃一顿好饭”是角色恢复体力（HP）和驱散疲劳状态的最自然方式，可与背包消耗品、体力状态形成深度联动。

### 缺憾与潜在难点（Limitations & Caveats）
1. **图鉴格子与时间流记录的逻辑协调（Grid vs Timeline）**：
   - 图2展示的是固定的 9 宫格图鉴。但用户需要记录“每天的早/中/晚饭”，如果页面只有 9 个格子，3 天就会填满。
   - **对策**：
     - 底部引入类似图2的**多分页圆点滑屏**（如按周或按天分页，一页记录当前周或近几天的三餐），或者支持**日期选择器快速切换天数**。
     - 每个未记录餐次的格子显示浅灰半透明的拱门槽位 `[ + 记一餐 ]`，点击即可上传并记录。
2. **手机照片比例与拱门徽章适配（Aspect Ratio & Cropping）**：
   - 用户上传的餐品照片多为 4:3 或 16:9，而图2的徽章为圆形/拱门形。
   - **对策**：采用 `object-fit: cover` 智能聚焦，卡片外圈自带粉嫩立体拱门描边，无论横图竖图均能精致嵌入。
3. **高频图片存储容量（IndexedDB 必选）**：
   - 每日三餐产生高频本地图片，严禁存入 `localStorage`（会导致 5MB 限制瞬间崩溃）。
   - **对策**：在 Dexie 中升级 `version(10)`，建立专用的 `rpg_meal_records` 表，无上限安全持久化。

### 未来可扩展性（Extensibility）
1. **AI 美食点评家（AI Foodie Critic）**：
   - 用户记录完菜品和评价后，调用已打通的 AI 接口，由“二次元暖心大厨/猫咪店长”发表一句趣味、治愈的每日吃播点评。
2. **三餐营养与 RPG 属性加成（Nutritional Buffs）**：
   - 吃高蛋白健身餐 ➔ 提示 `力量+1`；吃清爽轻食 ➔ 恢复 `体力+30`；吃犒赏甜点 ➔ 获得 `心流专注加成`。
3. **“今天吃什么”灵感转盘（Random Meal Decider）**：
   - 当纠结不知道吃什么时，基于历史打过 3 星的高分菜品，点击一键抽取今日美食灵感。

---

## 2. 供您选择的落地方向（Directions for User Choice）

### 方向 1【推荐·三餐时光册（深度还原图2 + 日历翻页）】
- **界面结构**：
  - **上方横条（对齐图2顶栏）**：
    - 默认展示当前所选菜式的大图标、记录日期（如 `2026.09.22 晚餐`）、美味星级（⭐⭐⭐）以及“一句话评价”。
    - 右侧保留粉色圆形关闭按钮（`✕`）。
  - **主体 3×3 拱门徽章格（对齐图2）**：
    - 9 个拱门格子：支持展示 3 天（每天早、中、晚各 3 格）或自定义分类（早餐栏、午餐栏、晚餐栏）。
    - 底部支持点击圆点翻页浏览历史日期的美食足迹。
    - 每个格子展示：用户自定义菜品大图、粉红缎带菜名（<=5字）、底部 1~3 颗金星。
    - 未记录的格子展示可爱的空态插槽，点击直接打开拍照/上传录入弹窗。
- **特点**：最纯正还原图2视觉，既有收集册的高颜值，又能清晰按早中晚查阅历史。

### 方向 2【美食大图鉴（永久菜谱图鉴库）】
- **界面结构**：
  - 9 宫格作为用户吃过的“常驻美食图鉴”，用户每打卡一种新菜品，解锁并点亮一个徽章格。
  - 点击某个菜品，上方横条显示“上次品尝日期”与最新评价。
- **特点**：偏向游戏图鉴收集（类似宝可梦图鉴），弱化每天固定打卡属性。

### 方向 3【拍立得日记瀑布流（Bento Polaroid Stream）】
- **界面结构**：
  - 弱化 3×3 九宫格，采用拍立得手账长卷形式，每天一张拍立得，上面贴着早中晚三道小菜照片。

---

## 3. 具体实施规划（拟改动代码与架构）

### 3.1 视觉图标开发（图1）
#### [NEW] [MealDiaryIcon.tsx](file:///c:/Users/Emily/Desktop/cloudfly-UI/轻拟物/src/components/apps/profile/components/MealDiaryIcon.tsx)
- 精准绘制图1粉白兔兔和果子图标：
  - 位于界面右侧、任务清单图标正下方（`top: 116px, right: 12px`）。
  - 木质长方托盘上盛放两只白白嫩嫩的小兔子和果子（红眼睛、浅绿耳尖）。
  - 粉红爱心半圆拱门背景与柔和外轮廓。

### 3.2 数据结构与 IndexedDB 存储升级
#### [MODIFY] [db.ts](file:///c:/Users/Emily/Desktop/cloudfly-UI/轻拟物/src/core/storage/db.ts)
- Dexie 数据库增加 `version(10)`：
  - 新增表 `rpg_meal_records: 'id, date, mealType, rating, updatedAt'`。
  - 记录字段包括：`id`, `date` (YYYY-MM-DD), `mealType` ('breakfast'|'lunch'|'dinner'|'snack'), `dishName`, `rating` (1~3), `review` (一句话评价), `imageData` (Base64), `createdAt`。
#### [NEW] [mealStorage.ts](file:///c:/Users/Emily/Desktop/cloudfly-UI/轻拟物/src/core/rpg/mealStorage.ts)
- 提供增删改查、按日期获取、预置美食图鉴数据接口。

### 3.3 美食手账主面板（图2）
#### [NEW] [MealDiarySheet.tsx](file:///c:/Users/Emily/Desktop/cloudfly-UI/轻拟物/src/components/apps/profile/components/MealDiarySheet.tsx)
- **上方横条（对齐图2顶栏）**：
  - 左侧半圆拱窗展示当前选中菜品大图。
  - 右侧清晰展示：记录日期（如 `09月22日 晚餐`）、美味三星标注、一句话点评（如“鲜香软糯，回味无穷！”）。
  - 右上角圆形粉色 `✕` 关闭按钮。
- **主体 3×3 和果子拱门窗格**：
  - 拱门式卡片槽位（Arched Windows），顶部蝴蝶结缎带装饰。
  - 用户自定义菜肴图片展示。
  - 下方粉嫩飘带菜品名称（严格 `<= 5 字`，如 `日式鳗鱼饭`、`草莓舒芙蕾`）。
  - 飘带下方 1~3 颗金星（⭐⭐⭐）。
  - 空态插槽点击唤起【记一餐】弹窗（选择餐别、上传图片、写名称、打星级、写一句话）。
- **底部分页指示器与左右翻页**：
  - 还原图2底部的圆点分页符与左右小箭头。

### 3.4 主界面挂载
#### [MODIFY] [ProfileApp.tsx](file:///c:/Users/Emily/Desktop/cloudfly-UI/轻拟物/src/components/apps/profile/ProfileApp.tsx)
- 在右侧任务图标下方挂载 `MealDiaryIcon` 按键。
- 引入 `MealDiarySheet` 模态弹窗。

---

## 4. 严格字数与设计规范
- 所有按钮、标签、标题与提示信息严格执行 **`<= 5 个汉字`** 规范（如 `美食手账`、`记一餐`、`早餐`、`午餐`、`晚餐`、`保存`、`删除`、`三星极品`、`美味可口`、`已记录`）。

---

## 5. 验证计划（Verification Plan）
- **构建编译验证**：运行 `npm run build` 确保 TypeScript 零报错，Vite 编译打包成功。
- **功能体验验证**：
  1. 验证右侧三个图标（立绘更换、亲缘图谱、任务清单、美食手账）垂直排布美观且不重叠。
  2. 点击美食图标展开图2样式的 3×3 拱门手账。
  3. 点击菜式，顶部横条动态联动刷新显示对应日期和评价。
  4. 测试点击空槽位上传新菜品、选择星级与写一句话评价，验证 IndexedDB 保存并刷新后数据完好留存。
