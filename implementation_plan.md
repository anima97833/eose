# 沉浸式 AI 陪伴“轻拟物”虚拟小手机实施策划书 (Detailed Implementation Plan)

本项目基于用户选定的核心路线（**Vite + React + TypeScript**、**现代纯轻拟物 Soft UI**、**本地优先 IndexedDB 存储并预留后端代理接口**）进行定制化架构设计与研发实施，并针对**全屏幕与多端浏览器视口深度适配**以及**分步验证**进行专项规程设计。

---

## 一、 核心技术选型与规格确立

| 维度 | 方案 | 落地规范 |
|---|---|---|
| **工程架构** | **Vite + React + TypeScript** | 极速秒级 HMR；严格类型定义；组件与状态高内聚解耦 |
| **设计语言** | **现代纯轻拟物（Neumorphism / Soft UI）** | 严格依照工作区参考图：温润米白基底（`#E9EEF5`）、左上双漫反射浮雕高光（白光）与右下柔和投影、凹陷式轨道输入框、立体圆形触控 |
| **存储策略** | **本地优先（IndexedDB / Dexie.js）** | 聊天记录、角色人设、配置完全保存在用户浏览器本地，刷新不丢失，零数据泄露 |
| **可扩展接口** | **预留选项 B（服务端代理/远程云端）** | 抽象 `ILLMAdapter` 与 `IStorageAdapter` 接口层，现行纯浏览器直连+本地存储，后续可直接插拔后端代理 |

---

## 二、 关键专项：全屏幕与多端浏览器视口深度适配

移动端浏览器（特别是国内常见的**夸克浏览器、QQ 浏览器、Via 浏览器、X 浏览器、Chrome Mobile、Safari**）存在显著的视口特性与兼容痛点：
* **痛点 1：底部工具栏遮挡与 100vh 塌陷**（传统 `100vh` 会被夸克/QQ 浏览器的底部导航栏挡住一部分内容）；
* **痛点 2：异形屏与安全区**（iPhone 刘海/灵动岛、安卓挖孔屏的安全区避让）；
* **痛点 3：桌面大屏 vs 移动真机双重场景体验不一**。

### 适配技术规程：
1. **双形态响应机制（Dual-Mode Adaptation）**：
   * **PC / 平板桌面端**：呈现为一部居中陈列的精巧轻拟物物理手机模型（约 390px × 844px 比例），包含微凸外壳、侧边物理按键、哑光金属质感边框；
   * **手机端（真实移动设备）**：自动切换为**真机全屏沉浸模式（True Fullscreen）**，去掉模拟外壳，界面直接贴合真实手机屏幕边缘。
2. **全能动态视口（Dynamic Viewport）技术**：
   * 采用现代 CSS `height: 100dvh` 并兼容降级为由 JavaScript 动态监听计算注入的 `--real-vh`；
   * `viewport-fit=cover` 配合 `padding-top: env(safe-area-inset-top)` 与 `padding-bottom: env(safe-area-inset-bottom)`，精准避让刘海屏与系统手势横条。

---

## 三、 分步交付与逐项停顿验证路线图 (Step-by-Step Roadmap)

按照您的要求：**每完成一个步骤即停止，向您详细汇报当前落地情况与验证效果，待您确认后再启动下一步。**

### [x] 【步骤 1：工程初始化 + 全屏/多浏览器视口适配 + 轻拟物光影系统】（已验收完成）
* 初始化 Vite + React + TypeScript 纯净工程；
* 实现双模式视口容器与自适应安全区避让；
* 编写核心轻拟物光影原子库与状态栏。

### [x] 【步骤 2：桌面系统 + 拟真可交互小组件搭建】（已验收完成）
* 精准复刻 `b9ab106eefede1a908e7b1e3a2fa9f48.jpg`：日期看板、87% 物理滑块、双网络开关、问候卡、音乐播放条；
* 升级为全页面所有图标支持自由拖拽换序，按压即时呈现深雕刻内凹阴影与弹簧回弹。

### [x] 【步骤 3：应用窗口管理器 + 系统与 API 配置 App】（已验收完成）
* 实现了 App 窗口生命周期路由（打开缩放进场、顶部返回键与底部 Home 退出手势）；
* 打造了纯正轻拟物风格的【系统与模型设置 App】：
  - 主区/分支双通道分线管理；
  - 直接从端点接口 `GET /models` 拉取模型下拉选择框；
  - 纯净的自定义 LocalStorage 预设库（可保存、调入、删除）；
  - 预留应用路线绑定勾选区与一键连通性探测。

### [x] 【步骤 4：核心通讯 App（仿微信）+ 角色卡人设系统】（当前已完成，等待验证）
* 仿微信极简大气即时通讯应用全面上线：
  - 底部标准双 Tab：💬 消息 / 👥 通讯录；
  - 消息列表：实时展示角色头像、最新消息预览与发送时间戳；
  - **通讯录 + 角色人设卡无缝打通**：
    * 点击通讯录中的 **`+ 添加角色人设卡`** 即可为角色编写核心 Prompt 设定、口癖语气、作息规律与身份标签；
    * 点击任意已有角色，可直接查看并随时微调其人设卡，支持删除角色；
  - **仿微信仿真会话视窗**：
    * 拟物轻量气泡，用户消息右侧淡蓝、角色消息左侧浅灰浮雕；
    * 顶部动态显示角色作息状态与“对方正在输入...”；
    * 实时对接设置中的大模型端点，支持真实流式打字输出；
    * 消息与角色全本地持久化。

### [ ] 【步骤 5：IndexedDB 本地持久化 + 预留云端抽象完备化 + 综合深度验收】（下一步）
* 接入 IndexedDB（Dexie.js）实现大数据量图片与会话持久化；
* 完成 `IStorageAdapter` 抽象与测试；
* 整体多浏览器（夸克、Chrome、Via 等）真实表现测试与全流程交付。
